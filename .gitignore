# Secrets / API tokens
.env
.cache
*.cache

# Python
.venv/
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# OS
.DS_Store

# Claude Code
CLAUDE.md
.claude/

# Data — covers and Spotify cache are reproducible via the scraper.
# albums_raw.csv + artists_raw.csv are kept (small, document the snapshot).
# data/splits/ is committed (small CSVs, reproducible but convenient to track).
# data/checkpoints/ contains large .pt model weights — gitignored.
data/covers/
data/covers_300/
data/spotify_cache.json
data/checkpoints/
